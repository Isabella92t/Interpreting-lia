# Auth0 configuration (public values — the client ID is not a secret in a native/SPA app).
# 1. Create an Auth0 application of type "Native" (or "Single Page Application" for web-only).
# 2. In the Auth0 application settings, add the app's callback URLs under
#    "Allowed Callback URLs" (and "Allowed Logout URLs"):
#      http://localhost:8081, interpretinglia://callback, and also add exp://ip-adress-of-the-simulator/--/callback
# 3. Fill in the values below and restart the Expo dev server.
EXPO_PUBLIC_AUTH0_DOMAIN=dev-apj4kux1p61jbawg.us.auth0.com
EXPO_PUBLIC_AUTH0_CLIENT_ID=L50ELY6zHGLPfUDHMHv6iy5PnzIQQD3I